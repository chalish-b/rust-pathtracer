[package]
name = "rust-pathtracer"
version = "0.1.0"
edition = "2024"

[dependencies]
glam = "0.32.0"
rand = "0.10.0"
rayon = "1.11.0"

# Good for profiling, don't enable for final build because of file size
# [profile.release]
# debug = true
